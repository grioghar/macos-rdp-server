<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.macosrdp.daemon</string>

    <key>ProgramArguments</key>
    <array>
        <string>/usr/local/sbin/macos-rdp-daemon</string>
        <string>--port</string>
        <string>3389</string>
    </array>

    <!-- Launch as root so CGEventPost and CoreAudio IO proc work system-wide. -->
    <key>UserName</key>
    <string>root</string>

    <key>RunAtLoad</key>
    <true/>

    <key>KeepAlive</key>
    <true/>

    <!-- Bind to the RDP port via launchd socket activation (optional). -->
    <key>Sockets</key>
    <dict>
        <key>RDPSocket</key>
        <dict>
            <key>SockFamily</key>
            <string>IPv6</string>
            <key>SockType</key>
            <string>Stream</string>
            <key>SockServiceName</key>
            <string>3389</string>
            <key>SockNodeName</key>
            <string>::</string>
        </dict>
    </dict>

    <!-- Set RDP_LOG_LEVEL=verbose or debug here for troubleshooting. -->
    <key>EnvironmentVariables</key>
    <dict>
        <key>RDP_LOG_LEVEL</key>
        <string>info</string>
    </dict>

    <key>StandardOutPath</key>
    <string>/var/log/macos-rdp-daemon.log</string>

    <key>StandardErrorPath</key>
    <string>/var/log/macos-rdp-daemon.error.log</string>

    <!-- Throttle relaunches after crashes. -->
    <key>ThrottleInterval</key>
    <integer>5</integer>

    <key>ProcessType</key>
    <string>Interactive</string>
</dict>
</plist>
